# 프로젝트 코드 리뷰

> **리포지토리**: FRONTENDBOOTCAMP-17th/board_pride
> **팀원**: 최유종, 김연수, 강재훈, 김준서, 이우현 (README 기준 추정)
> **리뷰 일자**: 2026-03-14

---

## 1. 프로젝트 개요

Vite + Tailwind CSS 기반 게시판 CRUD 웹 애플리케이션입니다. 빌드 도구, Prettier, PR 템플릿 등 실무 수준의 프로젝트 구성을 갖추고 있습니다.

### 파일 구성

| 파일 | 역할 |
|------|------|
| `index.html` | 프로젝트 안내 페이지 |
| `pages/signup.html` | 회원가입 (실시간 유효성 검증) |
| `pages/login.html` | 로그인 (Enter 키 지원, 자동 이동) |
| `pages/list.html` | 게시글 목록 (수정/삭제 버튼 포함) |
| `pages/write.html` | 게시글 작성 + 수정 (동일 페이지) |
| `pages/게시물상세정보.html` | 게시글 상세 (권한 체크 포함) |
| `pages/post.html` | 게시글 상세 (중복 파일) |

### 기술 스택

- Vite 7.3 + Tailwind CSS 4.1
- Pretendard 커스텀 폰트
- Prettier 코드 포매터
- npm-run-all (빌드 스크립트)

---

## 2. 잘한 점

### 2.1 프로젝트 설정 — 전체 팀 중 최고 수준

```
├── vite.config.js       # MPA 빌드 설정
├── .prettierrc.cjs      # 코드 스타일 통일
├── .gitignore           # node_modules, dist 등 제외
├── .github/PULL_REQUEST_TEMPLATE.md  # PR 체크리스트
├── package.json         # 빌드/프리뷰 스크립트
└── public/font/         # Pretendard 웹폰트
```

빌드 도구, 코드 포맷터, PR 템플릿, 커스텀 폰트까지 갖춘 팀은 이 팀이 유일합니다.

### 2.2 회원가입 유효성 검증 — 가장 정교한 구현

```javascript
const rules = {
  username: /^[A-Za-z0-9]{4,12}$/,
  password: /^(?=.*[A-Za-z])(?=.*\d)(?=.*[!@#$%^&*])[A-Za-z\d!@#$%^&*]{8,}$/,
  nickname: /^[가-힣]+$/,
};
```

- 비밀번호에 **영문+숫자+특수문자** 조합을 요구하는 정규식은 전체 팀 중 가장 강력합니다.
- 각 입력 필드 아래에 **인라인 에러 메시지**(`<span class="error">`)를 표시합니다.
- `validate()` 함수로 검증 로직을 분리했습니다.

### 2.3 작성/수정 통합 페이지 (write.html)

```javascript
const postId = params.get('id');

if (postId) {
  pageTitle.textContent = '게시글 수정';
  writeBtn.textContent = '수정하기';
  await loadPost(postId);
}
```

- URL 파라미터(`?id=`)로 작성/수정 모드를 전환합니다.
- 별도의 수정 페이지 없이 하나의 파일로 두 기능을 처리하는 효율적인 설계입니다.

### 2.4 게시글 작성자 권한 체크 (게시물상세정보.html)

```javascript
const me = await fetch(`${API_URL}/auth/me`, { ... });
if (me.ok) {
  const meData = await me.json();
  if (meData.data.nickname === postData.authorNickname) {
    postActions.style.display = 'flex';
  }
}
```

- 로그인한 사용자의 닉네임과 게시글 작성자를 비교하여 수정/삭제 버튼을 표시합니다.
- 비로그인 상태에서는 버튼이 완전히 숨겨집니다 (`display: none`).

### 2.5 XSS 방지 — 전 페이지 안전

`list.html`에서 `createElement` + `textContent`를 사용하고, 상세 페이지에서도 `textContent`를 사용합니다. 전체 팀 중 유일하게 XSS 취약점이 없습니다.

### 2.6 로그인 UX

```javascript
// Enter 키로 로그인
document.addEventListener('keypress', (e) => {
  if (e.key === 'Enter') {
    document.getElementById('loginBtn').click();
  }
});

// 토큰 필드 호환 처리
const token = data.data.accessToken || data.data.token;

// 로그인 후 1초 대기 후 이동
setTimeout(() => {
  window.location.href = "./list.html";
}, 1000);
```

- Enter 키 로그인, 빈 입력 검증, 성공 메시지 후 자동 이동까지 세심합니다.

### 2.7 반응형 디자인

```css
@media (max-width: 640px) {
  #post-list li { flex-direction: column; }
  #postActions { flex-direction: column; }
  #postActions button { width: 100%; }
}
```

- `list.html`과 상세 페이지에 모바일 대응 미디어 쿼리가 적용되어 있습니다.

### 2.8 시맨틱 HTML

```html
<time id="createdAt" datetime=""></time>
<fieldset><legend>게시물</legend></fieldset>
```

- `<time>` 요소에 `datetime` 속성, `<fieldset>`/`<legend>` 활용 등 시맨틱 마크업이 좋습니다.

---

## 3. 개선이 필요한 점

### 3.1 중복 상세 페이지 — 심각

`post.html`과 `게시물상세정보.html`이 거의 동일한 코드입니다.

| 차이점 | `post.html` | `게시물상세정보.html` |
|--------|-------------|-------------------|
| 수정 버튼 이동 경로 | `./게시글수정.html?id=${id}` | `./write.html?id=${id}` |
| 에러 시 이동 경로 | `./게시판목록.html` | `./게시판목록.html` |
| 외부 CSS 참조 | `./게시물상세정보.css` (없는 파일) | 없음 |

`post.html`은 존재하지 않는 파일들(`게시글수정.html`, `게시물상세정보.css`)을 참조합니다. `게시물상세정보.html`만 유지하고 `post.html`은 삭제하는 것이 좋습니다.

### 3.2 깨진 링크 — 심각

| 파일 | 참조 경로 | 실제 파일 |
|------|----------|----------|
| `post.html` 203행 | `./게시판목록.html` | 없음 (→ `list.html`) |
| `post.html` 250행 | `./게시글수정.html` | 없음 (→ `write.html`) |
| `게시물상세정보.html` 203행 | `./게시판목록.html` | 없음 (→ `list.html`) |
| `post.html` 8행 | `./게시물상세정보.css` | 없음 |

에러 발생 시 `게시판목록.html`로 이동하려 하지만 이 파일이 없어서 404가 됩니다.

### 3.3 한글 파일명

```
pages/게시물상세정보.html
```

한글 파일명은 URL 인코딩 문제를 일으킬 수 있습니다. `detail.html` 같은 영문명이 안전합니다.

### 3.4 list.html에서 권한 체크 없이 수정/삭제 버튼 노출

```javascript
// list.html — 모든 게시글에 수정/삭제 버튼이 표시됨
const editBtn = document.createElement('button');
editBtn.textContent = '수정';
const deleteBtn = document.createElement('button');
deleteBtn.textContent = '삭제';
```

`게시물상세정보.html`에서는 권한 체크를 하지만, `list.html`에서는 **모든 글**에 수정/삭제 버튼이 표시됩니다. 다른 사람의 글도 삭제를 시도할 수 있습니다 (API에서 403이 돌아오겠지만 UX가 좋지 않습니다).

### 3.5 Vite 빌드 설정과 실제 구조 불일치

```javascript
// vite.config.js — src/ 폴더를 스캔
...findAllHtmlFiles(path.resolve(__dirname, "src")),
```

HTML 파일들은 `pages/` 폴더에 있지만, `findAllHtmlFiles`는 `src/` 폴더를 스캔합니다. 빌드 시 페이지 파일들이 포함되지 않습니다.

```javascript
// 수정 필요
...findAllHtmlFiles(path.resolve(__dirname, "pages")),
```

### 3.6 `<title>` 오류

```html
<!-- 게시물상세정보.html, post.html -->
<title>게시글 삭제</title>
<!-- 실제로는 상세보기 페이지 -->
```

페이지 제목이 "게시글 삭제"로 되어 있지만 실제로는 상세보기 페이지입니다.

### 3.7 회원가입 후 즉시 리다이렉트

```javascript
result.textContent = '회원가입 성공!\n...';
result.style.color = 'green';
location.replace('./login.html');  // 즉시 이동 → 성공 메시지를 볼 수 없음
```

로그인 페이지처럼 `setTimeout`으로 잠시 대기 후 이동하면 사용자가 성공 메시지를 확인할 수 있습니다.

### 3.8 console.log 잔존

```javascript
// write.html
console.log('내 정보:', data);
console.log('게시글 정보:', data);
console.log('draft:', draftData);
console.log('publish:', publishData);
console.log('수정 결과:', updateData);
```

디버깅용 `console.log`가 다수 남아 있습니다.

---

## 4. 보안 관련

| 항목 | 상태 | 설명 |
|------|------|------|
| XSS | ✅ 안전 | 전 페이지 `textContent`/`createElement` 사용 |
| 입력 검증 | ✅ 우수 | 정규식 + 인라인 에러, 특수문자 포함 비밀번호 |
| 토큰 관리 | ✅ 양호 | 로그인 체크 후 폼 표시/숨김 |
| 권한 처리 | ⚠️ 부분적 | 상세 페이지는 양호, 목록 페이지는 미흡 |

---

## 5. Git 활용

### 브랜치 전략

```
main (안정 버전)
  └── develop (기본 브랜치)
        ├── feature/signup-refactor
        ├── feature/html-aboutPost
        ├── feature/rewrite
        ├── login
        ├── 23-list-수정
        └── refactor/total-html
```

### PR 활용

- **28개 PR** 생성 — 전체 팀 중 가장 활발한 PR 활용
- PR 템플릿에 유형 체크리스트, 컨벤션 확인, 이슈 연결 항목 포함
- `feat:`, `refactor:`, `docs:` 커밋 컨벤션 사용

### README

- 프로젝트 소개, 목표, 협업 방식, 브랜치 전략, 커밋 컨벤션, 디렉토리 구조까지 문서화
- 전체 팀 중 가장 완성도 높은 README

---

## 6. 전체 점수

| 항목 | 점수 | 코멘트 |
|------|------|--------|
| 프로젝트 구성 | ⭐⭐⭐⭐⭐ | Vite, Prettier, PR 템플릿, 커스텀 폰트 |
| 기능 완성도 | ⭐⭐⭐⭐ | CRUD 전체 구현, 작성/수정 통합 |
| 보안 | ⭐⭐⭐⭐⭐ | XSS 안전, 강력한 입력 검증 |
| UI/스타일 | ⭐⭐⭐⭐⭐ | 반응형, Pretendard 폰트, 일관된 디자인 |
| 코드 품질 | ⭐⭐⭐ | 중복 파일, 깨진 링크, console.log |
| Git 활용 | ⭐⭐⭐⭐⭐ | PR 28개, 템플릿, README, 컨벤션 |

---

## 7. 우선 수정 사항

1. **중복 파일 정리** — `post.html` 삭제, `게시물상세정보.html`만 유지 (또는 `detail.html`로 개명)
2. **깨진 링크 수정** — `게시판목록.html` → `list.html`, `게시글수정.html` → `write.html?id=`
3. **Vite 빌드 설정** — `findAllHtmlFiles` 대상을 `src/` → `pages/`로 변경
4. **list.html 권한 체크** — 내 글에만 수정/삭제 버튼 표시
5. **`<title>` 수정** — "게시글 삭제" → "게시글 상세"
6. **console.log 제거**
